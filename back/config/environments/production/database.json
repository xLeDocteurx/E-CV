{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.DATABASE_HOST || 'cluster0-0unpv.mongodb.net'}",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "${process.env.DATABASE_NAME || 'test'}",
        "username": "${process.env.DATABASE_USERNAME || 'ledocteur'}",
        "password": "${process.env.DATABASE_PASSWORD || 'TatcherIs100$Sexy'}",
        "srv": "${process.env.DATABASE_SRV || true}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || true}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || 'admin'}"
      }
    }
  }
}
